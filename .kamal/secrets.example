#!/usr/bin/env bash
# Copy to .kamal/secrets and set real values. Do not commit .kamal/secrets.
# Or ensure vars are in your environment (e.g. from .env.production) when running kamal.
export RAILS_MASTER_KEY="${RAILS_MASTER_KEY:-}"
export SECRET_KEY_BASE="${SECRET_KEY_BASE:-}"
export JWT_SECRET_KEY="${JWT_SECRET_KEY:-}"
